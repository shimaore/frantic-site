{
  "name": "frantic-site",
  "version": "1.0.0",
  "description": "CouchDB URL mapping to replication data",
  "main": "index.js",
  "scripts": {
    "build": "npm install coffeescript && coffee -c *.coffee.md",
    "pretest": "npm run build && coffee -c test/*.coffee.md",
    "prepublishOnly": "npm run build",
    "test": "nyc mocha"
  },
  "repository": {
    "type": "git",
    "url": "gitlab.com/shimaore/frantic-site"
  },
  "keywords": [
    "CouchDB",
    "replication"
  ],
  "author": "stephane@shimaore.net",
  "license": "Unlicense",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.0.2",
    "nyc": "^13.3.0"
  },
  "dependencies": {
    "coffeescript": "^2.4.0"
  }
}
